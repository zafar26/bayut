import Navbar from '../../../components/Navbar/Navbar';

const Listings = () => {
    return (
        <div>
            <Navbar selectedLink={'Listings'} />
            <div className="pt-16  h-screen ">LISTINGS</div>
        </div>
    );
};

export default Listings;
